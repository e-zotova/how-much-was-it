import React from "react";

function Result ({}) {

  const onCalculate = (e) => {
    
  }

  return (
    <div className="result" onCalculate={onCalculate}>
      Раньше было лучше
    </div>
  ); 
}

export default Result;